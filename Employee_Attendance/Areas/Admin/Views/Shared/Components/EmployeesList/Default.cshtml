@model List<EmployeeViewModel>
   
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>م</th>
                <th>name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var d in Model)
            {
            <tr>
                <td>@d.Id</td>
                <td>@d.UserName</td>
            </tr>
            }
        </tbody>
    </table>
